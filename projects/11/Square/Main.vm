// class Main

function Main.main 1
// var SquareGame game (local 0)
// static call SquareGame.new 
call SquareGame.new 0
pop local 0 // game =
push local 0 // game (implicit this push)
call SquareGame.run 1
push local 0 // game (implicit this push)
call SquareGame.dispose 1
return
